\documentclass[10pt]{article}
\usepackage[a2paper,margin=0.2cm]{geometry}
\usepackage[russian]{babel}
\usepackage[utf8]{inputenc}
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{latexsym}
\usepackage{enumerate}

\input{commands}


\begin{document}

\title{Super Proof}
\author{Anton}
\maketitle

\begin{center}
\step{ \Gamma, \exists x \varphi \vdash \psi }{\existsEp[,x < t\ \text{и вообще что угодно}]}
{
	\step{\Gamma, \varphi \vdash \psi}{\ax}{}
}
\DisplayProof
\end{center}

\begin{center}
	\step{\vdash (\forall x \varphi) \iff \neg \exists x \neg \varphi}{\andI}
	{
		\step{ \vdash \forall x \varphi \to \neg \exists x \neg \varphi}{\toI}
		{
			\step{\forall x \varphi \vdash \neg \exists x \neg \varphi}{\toI}
			{
				\step{\forall x \varphi, \exists x \neg \varphi \vdash 0}{\existsEp}
				{
					\step{\forall x \varphi, \neg \varphi \vdash 0}{\unary[2b]}
					{
						\step{\varphi, \neg \varphi}{\pc}{}
					}
				}
			}
		}
		\step{\vdash \neg \exists x \neg \varphi \to \forall x \varphi}{\toI}
		{
			\step{\neg \exists x \neg \varphi \vdash \forall x \varphi}{\forallI}
			{
				\step{\neg \exists x \neg \varphi \vdash \varphi}{\toE}
				{
					\step{\neg \exists x \neg \varphi \vdash \neg \neg \varphi \to \varphi}{\pc}{}
					\step{\neg \exists x \neg \varphi \vdash \neg \neg \varphi}{\toI}
					{
						\step{\neg \exists x \neg \varphi, \neg \varphi \vdash 0}{\toE}
						{
							\step{\neg \exists x \neg \varphi, \neg \varphi \vdash \neg \exists x \neg \varphi}{\R}{}
							\step{\neg \exists x \neg \varphi, \neg \varphi \vdash \exists x \neg \varphi}{\existsI}
							{
								\step{\neg \exists x \neg \varphi, \neg \varphi \vdash \neg \varphi}{\R}{}
							}
						}
					}
				}
			}
		}
	}
	\DisplayProof
\end{center}


\end{document}