\documentclass[10pt]{article}
\usepackage[a2paper,margin=0.2cm]{geometry}
\usepackage[russian]{babel}
\usepackage[utf8]{inputenc}
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{latexsym}
\usepackage{enumerate}

\input{commands}

\begin{document}

\title{Домашняя работа 3}
\author{Антон Афанасьев}
\maketitle

\begin{enumerate}
\item
	\begin{enumerate}
		\item 
			\AxiomC{$\Gamma \vdash a, b, \Delta$}
			\UnaryInfC{$\Gamma, a \downarrow b \vdash \Delta$}
			\DisplayProof\ \ \ 
			\AxiomC{$\Gamma, a \vdash \Delta$}
			\AxiomC{$\Gamma, b \vdash \Delta$}
			\BinaryInfC{$\Gamma \vdash a \downarrow b, \Delta$}
			\DisplayProof
		\item
			\AxiomC{$\Gamma \vdash a, \Delta$}
			\AxiomC{$\Gamma \vdash b, \Delta$}
			\BinaryInfC{$\Gamma, a \uparrow b \vdash \Delta$}
			\DisplayProof\ \ \ 
			\AxiomC{$\Gamma, a, b \vdash \Delta$}
			\UnaryInfC{$\Gamma \vdash a \uparrow b, \Delta$}
			\DisplayProof
	\end{enumerate}
	
\item Поигрался =)

\item 
\begin{enumerate}
	\item Пусть $\lnot A$ --- тавтология, тогда она выводима в классическом исчислении высказываний. По теореме Гливенко в интуиционистском исчислении выводимо $\lnot \lnot \lnot A$. Также в интуиционистском исчислении выводима формула $\lnot \lnot \lnot A \to  \lnot A$, а значит, $\lnot A$ тоже выводимо.
	
	\item Пусть $\Gamma \vdash 0$ выводимо в интуиционистском исчислении. Т.к. любой интуиционистский вывод является корректным выводом в классическом исчислении, то $\Gamma \vdash 0$ выводимо и в классическом.\\
	Пусть $\Gamma \vdash 0$ выводимо в классическом исчислении. $\Gamma \vdash 0 \Rightarrow\ \vdash \Gamma \to 0 \Rightarrow\ \vdash \lnot \Gamma$. В пункте (a) было показано, что любая тавтология такого вида выводима в интуиционистском исчислении. Также в нем работает лемма о дедукции, а значит выводимо $\Gamma \vdash 0$.
	\end{enumerate}
	
\item Выводы в интуиционистском исчислении\\
\begin{enumerate}
	\item $\vdash \lnot \lnot (( A \to B) \to \lnot A \lor B)$\\
	\begin{center}
	\step{\vdash \lnot \lnot (( A \to B) \to \lnot A \lor B)}{toI}
	{
		\step{\lnot (( A \to B) \to \lnot A \lor B) \vdash 0}{toE}
		{
			\step{\lnot (( A \to B) \to \lnot A \lor B) \vdash (( A \to B) \to \lnot A \lor B) \to 0}{R}{}
			
			\step{\lnot (( A \to B) \to \lnot A \lor B) \vdash (A \to B) \to \lnot A \lor B}{toI}
			{
				\step{\lnot (( A \to B) \to \lnot A \lor B), A \to B \vdash \lnot A \lor B}{orIl}
				{
					\step{\lnot (( A \to B) \to \lnot A \lor B), A \to B \vdash \lnot A}{toI}
					{
						\step{\lnot (( A \to B) \to \lnot A \lor B), A \to B, A \vdash 0}{toE}
						{
							\step{\lnot (( A \to B) \to \lnot A \lor B), A \to B, A \vdash (( A \to B) \to \lnot A \lor B) \to 0}{R}{}
							
							\step{\lnot (( A \to B) \to \lnot A \lor B), A \to B, A \vdash (A \to B) \to \lnot A \lor B}{toI}
							{
								\step{\lnot (( A \to B) \to \lnot A \lor B), A \to B, A \vdash \lnot A \lor B}{orIr}
								{
									\step{\lnot (( A \to B) \to \lnot A \lor B), A \to B, A \vdash B}{toE}
									{
										\step{\lnot (( A \to B) \to \lnot A \lor B), A \to B, A \vdash A \to B}{R}{}
										\step{\lnot (( A \to B) \to \lnot A \lor B), A \to B, A \vdash A}{R}{}
									}
								}
							}
						}
					}
				}
			}
		}
	}
	\DisplayProof
	\end{center}
	
	\item $\vdash \lnot \lnot (\lnot \lnot A \to A)$ \\
	\begin{center}
	\step{\vdash \lnot \lnot (\lnot \lnot A \to A)}{toI}
	{
		\step{\lnot (\lnot \lnot A \to A) \vdash 0}{toE}
		{
			\step{\lnot (\lnot \lnot A \to A) \vdash  (\lnot \lnot A \to A) \to 0}{R}{}
			\step{\lnot (\lnot \lnot A \to A) \vdash \lnot \lnot A \to A}{toI}
			{
				\step{\lnot (\lnot \lnot A \to A), \lnot \lnot A \vdash A}{orE}
				{
					\step{\lnot (\lnot \lnot A \to A), \lnot \lnot A \vdash A \lor \lnot A}{orIr}
					{
						\step{\lnot (\lnot \lnot A \to A), \lnot \lnot A \vdash \lnot A}{toI}
						{
							\step{\lnot (\lnot \lnot A \to A), \lnot \lnot A, A \vdash 0}{toE}
							{
								\step{\lnot (\lnot \lnot A \to A), \lnot \lnot A, A \vdash (\lnot \lnot A \to A) \to 0}{R}{}
								\step{\lnot (\lnot \lnot A \to A), \lnot \lnot A, A \vdash \lnot \lnot A \to A}{toI}
								{
									\step{\lnot (\lnot \lnot A \to A), \lnot \lnot A, A \vdash A}{R}{}
								}
							}
						}
					}
					
					\step{\lnot (\lnot \lnot A \to A), \lnot \lnot A, A \vdash A}{R}{}
					\step{\lnot (\lnot \lnot A \to A), \lnot \lnot A, \lnot A \vdash A}{botE}
					{
						\step{\lnot (\lnot \lnot A \to A), \lnot \lnot A, \lnot A \vdash 0}{toE}
						{
							\step{\lnot (\lnot \lnot A \to A), \lnot \lnot A, \lnot A \vdash \lnot A \to 0}{R}{}
							\step{\lnot (\lnot \lnot A \to A), \lnot \lnot A, \lnot A \vdash \lnot A}{R}{}
						}
					}
				}
			}
		}
	}
	\DisplayProof
	\end{center}
	
	\item $\vdash \lnot \lnot ((\lnot A \to \lnot B) \to B \to A)$
	\begin{center}
	\step{\lnot ((\lnot A \to \lnot B) \to B \to A), \lnot A \to \lnot B, B \vdash A \lor \lnot A}{orIl}
	{
		\step{\lnot ((\lnot A \to \lnot B) \to B \to A), \lnot A \to \lnot B, B \vdash \lnot A}{toI}
		{
			\step{\lnot ((\lnot A \to \lnot B) \to B \to A), \lnot A \to \lnot B, B, A \vdash 0}{toE}
			{
				\step{\lnot ((\lnot A \to \lnot B) \to B \to A), \lnot A \to \lnot B, B, A \vdash ((\lnot A \to \lnot B) \to B \to A) \to 0}{R}{}
				\step{\lnot ((\lnot A \to \lnot B) \to B \to A), \lnot A \to \lnot B, B, A \vdash (\lnot A \to \lnot B) \to B \to A}{toI}
				{
					\step{\lnot ((\lnot A \to \lnot B) \to B \to A), \lnot A \to \lnot B, B, A \vdash B \to A}{toI}
					{
						\step{\lnot ((\lnot A \to \lnot B) \to B \to A), \lnot A \to \lnot B, B, A \vdash A}{R}{}
					}
				}
			}
		}
	}
	\DisplayProof
	\vspace*{2\baselineskip}
	
	\step{\lnot ((\lnot A \to \lnot B) \to B \to A), \lnot A \to \lnot B, B, \lnot A \vdash A}{botE}
	{
		\step{\lnot ((\lnot A \to \lnot B) \to B \to A), \lnot A \to \lnot B, B, \lnot A \vdash 0}{toE}
		{
			\step{\lnot ((\lnot A \to \lnot B) \to B \to A), \lnot A \to \lnot B, B, \lnot A \vdash B \to 0}{toE}
			{
				\step{\lnot ((\lnot A \to \lnot B) \to B \to A), \lnot A \to \lnot B, B, \lnot A \vdash \lnot A \to \lnot B}{R}{}
				\step{\lnot ((\lnot A \to \lnot B) \to B \to A), \lnot A \to \lnot B, B, \lnot A \vdash \lnot A}{R}{}
			}
			
			\step{\lnot ((\lnot A \to \lnot B) \to B \to A), \lnot A \to \lnot B, B, \lnot A \vdash B}{R}{}
		}
	}
	\DisplayProof
	\vspace*{2\baselineskip}
	
	\step{\lnot ((\lnot A \to \lnot B) \to B \to A) \vdash (\lnot A \to \lnot B) \to B \to A}{toI}
	{
		\step{\lnot ((\lnot A \to \lnot B) \to B \to A), \lnot A \to \lnot B \vdash B \to A}{toI}
		{
			\step{\lnot ((\lnot A \to \lnot B) \to B \to A), \lnot A \to \lnot B, B \vdash A}{orE}
			{
				\step{\lnot ((\lnot A \to \lnot B) \to B \to A), \lnot A \to \lnot B, B \vdash A \lor \lnot A}{ax}{}
				\step{\lnot ((\lnot A \to \lnot B) \to B \to A), \lnot A \to \lnot B, B, A \vdash A}{R}{}
				\step{\lnot ((\lnot A \to \lnot B) \to B \to A), \lnot A \to \lnot B, B, \lnot A \vdash A}{ax}{}
			}
		}
	}
	\DisplayProof
	\vspace*{2\baselineskip}
	
	\step{\vdash \lnot \lnot ((\lnot A \to \lnot B) \to B \to A)}{toI}
	{
		\step{\lnot ((\lnot A \to \lnot B) \to B \to A) \vdash 0}{toE}
		{
			\step{\lnot ((\lnot A \to \lnot B) \to B \to A) \vdash ((\lnot A \to \lnot B) \to B \to A) \to 0}{R}{}
			\step{\lnot ((\lnot A \to \lnot B) \to B \to A) \vdash (\lnot A \to \lnot B) \to B \to A}{ax}{}
		}
	}
	\DisplayProof
	\end{center}
\end{enumerate}
\end{enumerate}

\end{document}